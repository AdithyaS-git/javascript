const radius = 5;
//area=PI*(r^2)
const area =Math.PI*Math.pow(radius,2)
console.log(`The area of circle is ${area.toFixed(2)}`)


const num=7.3;

console.log(`Rounded number is  ${Math.round(num)}`)
console.log(`Ceil : ${Math.ceil(num)}`);
console.log(`Floor : ${Math.floor(num)}`);

//square root
const value= 25;
console.log(`The sqrt is :${value} is ${Math.sqrt(value)}`)